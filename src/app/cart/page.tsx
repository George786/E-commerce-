'use client'

import { useEffect } from 'react'

export default function CartPage() {

	// Fetch cart from backend on page load
	useEffect(() => {
		fetchCart()
	}, [fetchCart])

		return (
					</div>

						<button
						>
						</button>
					</div>

				</div>
				</div>
			</div>
	)
}
